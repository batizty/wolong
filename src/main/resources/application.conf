akka {
  loglevel = "DEBUG"
  event-handlers = ["akka.event.slf4j.Slf4jEventHandler"]
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  stdout-loglevel = "DEBUG"

  actor {
    debug {
      receive = off
      lifecycle = off
    }
    //    provider = "akka.cluster.ClusterActorRefProvider"
    //    default-dispatcher {
    //      # Throughput for default Dispatcher, set to 1 for as fair as possible
    //      throughput = 1
    //    }
  }

  remote {
    # The port clients should connect to. Default is 2552.
    netty.tcp.port = 2552
  }
}

spray.can.server {
  request-timeout = 1s
}

service {
  host = "localhost"
  port = 8080
  expiretime = 10
}

data {
  soruce = "web"
}

db {
  host = "10.77.136.64"
  port = 3306
  name = "datasys_monitor"
  user = "hadoop"
  password = "hadoop"
}

web {
  url_prefix = "http://mlplat.intra.weibo.com/math"
  retry = 3
  timeout = 5

  user_url = "user"
  group_url = "getGroup"
  task_url = "getTask"
}

mesos {
  master = "10.77.136.42:5050"
  default_user = "hadoop"
  framework {
    name = "weibo.wolong"
  }
}